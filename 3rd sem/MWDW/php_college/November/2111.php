
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Document</title>
</head>
<body>
    <pre>
        <?php
            $demo = 'demo.txt';

            // readfile($demo);

            // $f1 = fopen($demo, 'r+');
            // $txt = <<<_END
            // This is a test of the emergency broadcast system.

            // _END;
            // echo fread($f1, filesize($demo));
            // fwrite($f1, $txt);
            // fclose($f1);
            // readfile($demo);

            // copy($demo, 'd.txt');

            // unlink('d.txt');

            // rename('d1.txt', 'd1.txt');    //also can change directory

            // $f2 = fopen($demo, 'r+');
            // $txt = fgets($f2);
            // fseek($f2, 2, SEEK_SET);
            // fwrite($f2, $txt);
            // fclose($f2);
            // readfile($demo);

            // $f2 = fopen($demo, 'r+');
            // $txt = fgets($f2);
            // fseek($f2, 2, SEEK_CUR);
            // fwrite($f2, $txt);
            // fclose($f2);
            // readfile($demo);

            // $f2 = fopen($demo, 'r+');
            // $txt = fgets($f2);
            // if(flock($f2, LOCK_EX)) {
            //     fseek($f2, 0, SEEK_END);
            //     fwrite($f2, $txt);
            //     flock($f2, LOCK_UN);
            // }
            // fclose($f2);
            // readfile($demo);
        ?>
    </pre>
</body>
</html>

